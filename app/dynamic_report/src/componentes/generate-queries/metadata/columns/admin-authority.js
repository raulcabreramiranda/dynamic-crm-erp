"use strict";

const columns = {
  AdminAuthority: {
        name: "AdminAuthority",
        label: "AdminAuthority",
        columns: [
          { name: "id", label: "id", type: "Integer" },
          { name: "name", label: "name", type: "String" },
          
          
        ],
      },
};

exports["default"] = columns;
